{{> customerNavBar}}

<h1>Order list</h1>

<div>
  <ul class="order-list">
    <div class="orders-list">


      {{#each orders}}
      <div class="order-list">
        <ul>
          <li>Address: {{this.address.addressLine1}} {{this.address.addressLine2}}</li>
          <li>Phone Number: {{this.phoneNumber}}</li>
          <li>Allergies: {{this.allergies}} </li>
          <li>Dietary Requirements: {{this.dietaryRequirements}} </li>
          <li>Budget: {{this.budget}}</li>
          {{#unless this.userId.isCustomer}}
          <div class="field submit">
            <a href="/orderlist/{{this._id}}/view">
              <button id="edit-btn">View Order</button>
            </a>
          </div>
          {{else}}
          <div class="field submit">
            <a href="/orderlist/{{this._id}}/edit">
              <button id="edit-btn">Edit Order</button>
            </a></div>
          <form method="POST" action="/orderlist/{{this._id}}/delete">
            <div class="field submit">
              <button type="submit">Delete Order</button>
            </div>
          </form>
          {{/unless}}
        </ul>
      </div>
      {{/each}}

    </div>



    {{!-- addressLine1

    addressLine2
    city
    postcode
    phoneNumber
    allergies
    dietaryRequirements:
    budget
    price
    numberOfFoodeez
    willServe: {
    isCompleted --}}